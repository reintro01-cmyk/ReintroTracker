# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (never commit real values; keep the example)
.env
.env.*
!.env.example

# Claude Code local settings
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local DB connection strings (never commit) — dev + prod
pipeline/*db_url

# Python bytecode caches
__pycache__/
*.pyc

# Supabase CLI scratch
supabase/.temp/

# Generated pipeline artifacts (re-runnable from scripts; *.md reports are kept)
pipeline/out/*.sql
pipeline/out/*.json

# Local Claude Code skills install
.agents/

# One-off connection-error scratch file
conn_err.txt

# Python venv for pipeline tooling
pipeline/.venv/
